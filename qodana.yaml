version: "1.0"

linter: jetbrains/qodana-js:2025.1

profile:
  name: qodana.recommended

exclude:
  - name: All
    paths:
      - node_modules
      - dist
      - coverage
      - .angular
      - public

include:
  - name: All

failThreshold: 0

bootstrap: npm ci
